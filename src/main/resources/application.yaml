ktor:
    application:
        modules:
            - com.salles.ApplicationKt.module
    deployment:
        port: 8080

db:
    name: my_inflation
    url: ${DB_URL:jdbc:postgresql://localhost:5432/my_inflation}
    user: ${DB_USER:salles}
    password: ${DB_PASSWORD:salles}
    pool:
        maximumPoolSize: 10
